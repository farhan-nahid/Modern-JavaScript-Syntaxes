// ternary operator

const age =5;

let type = age>=18 ? 'Adult' : age<=9 ? 'Child' :'Young'
console.log(type);

// if else er kaj choto kora. but we can use it maximum 2 condition otherwise it will be complex.